name: Build and publish NuGet

on:
  push:
    branches: [ github-actions ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.1.101
    - name: Build with dotnet
      run: dotnet build SpeckleCore\SpeckleCore.sln --configuration Release --verbosity minimal
#     - name: Publish NuGet
#       uses: rohith/publish-nuget@v2.1.2
#       with:
#         PROJECT_FILE_PATH: SpeckleCore\SpeckleCore.csproj
#         NUGET_KEY: ${{secrets.NUGET_API_KEY}}

#${{ github.run_id }}
    
